-- =============================================================================
-- BOM ERP: bomerp-acopio-oro
-- BD2 - Creación de Esquema BOM_CATALOGO y Tablas CATEGORIA / PRODUCTO
-- Propietario: BOM_CATALOGO | Usuario App Backend: BOMERP_APP
-- =============================================================================

ALTER SESSION SET CONTAINER = XEPDB1;

-- 1. Crear Esquema BOM_CATALOGO
CREATE USER BOM_CATALOGO IDENTIFIED BY "123456" QUOTA UNLIMITED ON USERS;
GRANT CREATE SESSION, CREATE TABLE, CREATE VIEW, CREATE PROCEDURE, CREATE TRIGGER TO BOM_CATALOGO;

-- 2. Tabla CATEGORIA (Esquema BOM_CATALOGO)
CREATE TABLE BOM_CATALOGO.CATEGORIA (
    ID_CATEGORIA NUMBER GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    NOMBRE       VARCHAR2(80) NOT NULL UNIQUE,
    DESCRIPCION  VARCHAR2(200)
);

-- 3. Tabla PRODUCTO (Esquema BOM_CATALOGO)
CREATE TABLE BOM_CATALOGO.PRODUCTO (
    ID_PRODUCTO  NUMBER GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    NOMBRE       VARCHAR2(120) NOT NULL,
    PRECIO       NUMBER(10, 2) NOT NULL,
    STOCK        NUMBER NOT NULL
);

-- 4. Permisos DML a BOMERP_APP
GRANT SELECT, INSERT, UPDATE, DELETE ON BOM_CATALOGO.CATEGORIA TO BOMERP_APP;
GRANT SELECT, INSERT, UPDATE, DELETE ON BOM_CATALOGO.PRODUCTO TO BOMERP_APP;

-- 5. Semilla inicial de datos en BOM_CATALOGO
INSERT INTO BOM_CATALOGO.CATEGORIA (NOMBRE, DESCRIPCION) VALUES ('Insumos Acopio', 'Reactivos y materiales para control de ley de oro');
INSERT INTO BOM_CATALOGO.CATEGORIA (NOMBRE, DESCRIPCION) VALUES ('Equipos Fundición', 'Crisoles y balanzas analíticas de precisión');

INSERT INTO BOM_CATALOGO.PRODUCTO (NOMBRE, PRECIO, STOCK) VALUES ('Ácido Nítrico 1L', 45.50, 50);
INSERT INTO BOM_CATALOGO.PRODUCTO (NOMBRE, PRECIO, STOCK) VALUES ('Balanza Digital 0.001g', 850.00, 10);

COMMIT;
